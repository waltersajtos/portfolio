version: '2'
services:
  sveltekit:
    image: aamservices/pm2-sveltekit:dev
    restart: unless-stopped
    volumes:
      - sveltekit_data:/
    networks:
      - internal
    ports:
      - 3000
volumes:
  sveltekit_data:
    driver: local
